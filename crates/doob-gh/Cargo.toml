[package]
name = "doob-gh"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
doob-core = { path = "../doob-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
toml = "0.8"
chrono = "0.4"
dirs-next = "2"
